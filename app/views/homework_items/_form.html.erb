<%= form_for(@homework_item) do |f| %>
  <% if @homework_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@homework_item.errors.count, "error") %> prohibited this homework_item from being saved:</h2>

      <ul>
      <% @homework_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :done %><br />
    <%= f.check_box :done %>
  </div>
  <div class="field">
    <%= f.label :dueDate %><br />
    <%= f.date_select :dueDate %>
  </div>
  <div class="field">
    <%= f.label :homework_id %><br />
    <%= f.number_field :homework_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
